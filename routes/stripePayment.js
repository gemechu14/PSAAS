// const express = require("express");
// const router = express.Router();
// const stripeController = require("../controllers/stripePayment.js");

// router.post("/", stripeController.create_Payment);
// router.get("/pay", stripeController.pay);
// router.post("/charge", stripeController.charge);
// module.exports = router;
